/*
오픈AI 에서 사과의 정의를 쓰게 하고,
flux모델로 사과의 사진을 생성하고, 가져와서 저장하는 코드

 */
async function main() {
    console.log("start");
}

main().catch(err => {
    console.error(err);
    process.exit(1);
});

export {};
