/*
오픈AI 에서 사과(apple)의 정의, 역사, 요리들 쓰게하기
flux모델로 사과(apple)의 사진을 생성하고, 가져와서 저장하는 코드 -> 포켓베이스(서버)

 */
async function main() {
    console.log("start");

    // 사과의 정의를 가져오기



}

main().catch(err => {
    console.error(err);
    process.exit(1);
});

export {};
